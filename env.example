# Family PIN for accessing the app (set this in Railway environment variables)
# Leave unset for local development (no password required)
AUTH_PIN=your-pin-here

# Path to store the SQLite database (set to your Railway volume mount path)
# Leave unset for local development (uses project root)
DATA_DIR=/data
